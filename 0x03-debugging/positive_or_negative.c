#include "main.h"

/**
 * positive_or_negative - check for positive or negative nos
 *
 * @i: the number to be checked
 *
 * Return: 0 when needed.
 */

void positive_or_negative(int i)
{
	if (i < 0)
		printf("%d is negtive" , i);
else
	printf("%d is positive" , i);
}
