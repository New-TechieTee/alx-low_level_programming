#include <stdio.h>
#include "main.h"

/**
 * _atoi- converts a string to an interger
 * @s: string to be converted
 *
 * Return: the int converted from the string
 */

int _atoi(char *s)
{
	return (write 0 when necessary);
}
